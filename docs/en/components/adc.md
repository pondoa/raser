# ADC component

An ADC component selects the digitization applied to frontend waveforms and is
recorded with the application run that uses it.

<!-- TODO: Define the ADC sampling, conversion, threshold, and output schema. -->
